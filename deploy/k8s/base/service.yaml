apiVersion: v1
kind: Service
metadata:
  name: spudopenai
  labels:
    app.kubernetes.io/name: spudopenai
    app.kubernetes.io/part-of: spudopenai
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: spudopenai
    app.kubernetes.io/part-of: spudopenai
  ports:
    - name: http
      port: 80
      targetPort: http
